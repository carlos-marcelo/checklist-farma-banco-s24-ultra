










































Looking at the code you provided, I can see you want to add a `handleFinalize` function. However, I notice that the previous code you showed only contains `handleRecountAllDivergences`.

To apply your edit, I'll add the `handleFinalize` function after the existing `handleRecountAllDivergences` function:

